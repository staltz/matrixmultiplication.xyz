import * as FreeStyle from 'free-style';

const styles = FreeStyle.create();

styles.registerRule('body', {
  'margin': 0,
  'font-family': `'Source Sans Pro', serif`,
  'font-weight': '400',
});

export default styles;